<h1>Add a new recipe to <%= @category.name %></h1>

<%= simple_form_for [@category, @bookmark] do |f| %>
  <%= f.association :recipe %>
  <%= f.input :comment %>
  <%= f.submit "Add Bookmark" %>
<% end %>
<%= link_to "Back to Category", category_path(@category) %>
